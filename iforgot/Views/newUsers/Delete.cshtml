@model iforgot.Models.newUsers

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>newUsers</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.newUser_email)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.newUser_email)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.newUser_token)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.newUser_token)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.newUser_expires)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.newUser_expires)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
